{
  "name": "devlens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.mjs chromium --prod && node build.mjs firefox --prod",
    "build:dev": "node build.mjs chromium --dev && node build.mjs firefox --dev",
    "build:chromium": "node build.mjs chromium",
    "build:firefox": "node build.mjs firefox",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^20.14.0",
    "@types/webextension-polyfill": "^0.12.5",
    "esbuild": "^0.23.0",
    "jsdom": "^24.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
